<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>io.axoniq.axonserver</groupId>
    <artifactId>axonserver-ee</artifactId>
    <packaging>pom</packaging>
    <version>4.1.4-SNAPSHOT</version>

    <name>AxonIQ Axon Server EE</name>

    <modules>
        <module>axonserver-connector-api</module>
        <!--<module>axonserver</module>-->
        <module>sample-applications/sample-command-processor-axonhub-client</module>
        <module>sample-applications/sample-command-processor-axonserver-client</module>
        <module>axonserver-internal-grpc</module>
        <!--<module>axonserver-grpc</module>-->
        <module>axonserver-enterprise</module>
        <module>axonserver-migration</module>
        <!--<module>axonserver-cli</module>-->
        <module>axonserver-compatibility-tests</module>
        <module>package-axonserver-enterprise</module>
        <module>axondb-migration</module>
        <!--<module>plugins/axonserver-metrics-graphite</module>-->
        <!--<module>plugins/axonserver-metrics-prometheus</module>-->
        <!--<module>plugins/axonserver-rabbitmq-connector</module>-->
        <!--<module>plugins/axonserver-kafka-connector</module>-->
        <module>axonserver-cluster</module>
        <!--<module>query-parser</module>-->
    </modules>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <java.version>1.8</java.version>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>

        <product.name>AxonServer</product.name>

        <kotlin.version>1.1.61</kotlin.version>
        <kotlin.compiler.incremental>true</kotlin.compiler.incremental>
        <!--
            Please note that there are dependencies between the gRPC and Netty TcNative versions.
            gRPC is quite specific about the version of Netty TcNative, so check the dependencies on
            https://github.com/grpc/grpc-java/blob/master/SECURITY.md
        -->
        <grpc.version>1.19.0</grpc.version>
        <netty.tcnative.version>2.0.20.Final</netty.tcnative.version>

        <netty.version>4.1.32.Final</netty.version>
        <axonserver.api.version>4.1</axonserver.api.version>
        <axonserver.se.version>4.1.4-SNAPSHOT</axonserver.se.version>
        <axon.version>4.1.1</axon.version>
        <projectreactor.version>3.1.7.RELEASE</projectreactor.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-bom</artifactId>
                <version>${netty.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>2.1.2.RELEASE</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>io.axoniq</groupId>
                <artifactId>axon-server-api</artifactId>
                <version>${axonserver.api.version}</version>
            </dependency>

            <dependency>
                <groupId>junit</groupId>
                <artifactId>junit</artifactId>
                <version>4.12</version>
            </dependency>
            <!-- https://mvnrepository.com/artifact/javax.inject/javax.inject -->
            <dependency>
                <groupId>javax.inject</groupId>
                <artifactId>javax.inject</artifactId>
                <version>1</version>
            </dependency>

            <dependency>
                <groupId>io.projectreactor</groupId>
                <artifactId>reactor-core</artifactId>
                <version>${projectreactor.version}</version>
            </dependency>

            <dependency>
                <groupId>org.jetbrains.kotlin</groupId>
                <artifactId>kotlin-stdlib</artifactId>
                <version>${kotlin.version}</version>
            </dependency>
            <dependency>
                <groupId>ch.qos.logback</groupId>
                <artifactId>logback-classic</artifactId>
                <version>1.1.11</version>
            </dependency>
            <dependency>
                <groupId>ch.qos.logback</groupId>
                <artifactId>logback-core</artifactId>
                <version>1.1.11</version>
            </dependency>
            <dependency>
                <groupId>io.axoniq.axonserver</groupId>
                <artifactId>axonserver-cluster</artifactId>
                <version>${project.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <artifactId>maven-release-plugin</artifactId>
                <version>2.5.3</version>
                <configuration>
                    <mavenExecutorId>forked-path</mavenExecutorId>
                    <localCheckout>true</localCheckout>
                    <goals>deploy</goals>
                    <pushChanges>false</pushChanges>
                    <autoVersionSubmodules>true</autoVersionSubmodules>
                    <arguments>-Dmaven.javadoc.skip=true -Dsource.skip=true</arguments>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <scm>
        <connection>scm:git:git://github.com/AxonIQ/axon-server.git</connection>
        <developerConnection>scm:git:git@github.com:AxonIQ/axon-server.git</developerConnection>
        <url>https://github.com/AxonIQ/axon-server</url>
        <tag>axonserver-4.1</tag>
    </scm>

    <distributionManagement>
        <snapshotRepository>
            <id>axoniq-nexus</id>
            <url>https://dev-nexus.axoniq.io/repository/products-snapshots</url>
            <uniqueVersion>false</uniqueVersion>
        </snapshotRepository>
        <repository>
            <id>axoniq-nexus</id>
            <url>https://dev-nexus.axoniq.io/repository/products-releases</url>
            <uniqueVersion>true</uniqueVersion>
        </repository>
    </distributionManagement>

    <repositories>
        <repository>
            <id>axoniq-nexus</id>
            <url>https://dev-nexus.axoniq.io/repository/products/</url>
        </repository>
        <repository>
            <id>sonatype-snapshots</id>
            <url>https://dev-nexus.axoniq.io/repository/sonatype-snapshots/</url>
            <releases><enabled>false</enabled></releases>
            <snapshots><enabled>true</enabled></snapshots>
        </repository>
    </repositories>

    <organization>
        <name>AxonIQ BV</name>
        <url>http://axoniq.io</url>
    </organization>


    <developers>
        <developer>
            <name>Marc Gathier</name>
            <roles>
                <role>Developer</role>
            </roles>
        </developer>
    </developers>

</project>
