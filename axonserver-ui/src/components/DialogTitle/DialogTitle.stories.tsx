import React from 'react';
import { DialogTitle } from './DialogTitle';

export default {
  title: 'Components/DialogTitle',
  component: DialogTitle,
};

export const Default = () => (
  <DialogTitle>Hello from dialog title!</DialogTitle>
);
